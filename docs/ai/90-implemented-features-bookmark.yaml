schema_version: 1
document: implemented_features_bookmark
executor_capture_integration_2026_08_27:
  status: software_validated_physical_retest_pending
  capability: The 200 Hz point scheduler approval cancel hold diagnostics telemetry and capture-service responses remain independently schedulable while one explicit lock preserves state-machine ownership. Capture one's saved record may carry a fresh classified occlusion into the exact semantic acceptance barrier for up to 75 seconds; qualified labels continue and every unsafe unknown stale malformed or later non-CLEAR case remains fail-closed.
  unchanged: [Tesseract geometry and path, ray ranking and culling, plan hashes, authorization, runtime freshness, point order, following error, 20 Hz policy, real-motion default, PiPER command owner]
  evidence: [99 focused configuration capture and executor tests, 881 complete mobile tests, 165 complete Tesseract tests, 34 ROS-free GroundingDINO and worker tests, mobile and Tesseract symlink builds, all 43 functional mobile CTests, live proposal-only control-load service smoke, no joint-command topic]
rough_acquisition_partial_target_lock_2026_08_27:
  status: software_validated_physical_retest_pending
  capability: Rough acquisition can form a measured lock from a partially clipped but semantically colour mask and depth-qualified green cube. Clean-view box proportions remain mandatory for every non-rough heavy refresh.
  evidence: [physical task eaa02 archived artifact, exact 1.637 aspect replay, 34 focused tests, changed Python compile]
  preserved: [semantic confidence, green appearance, SAM2 support, aligned depth, tracking lock, capture framing, Tesseract, authorization, runtime safety, real-motion default]
capture_one_occlusion_barrier_2026_08_27:
  status: software_validated_physical_retest_pending
  capability: The dedicated semantic occlusion probe is deferred from rough lock to the first settled target-facing RGB-D record. The first alignment may enter from an offset acquired look but must safely enter the normal 20-degree cone and finish within 5 degrees. Capture one remains pending and cannot seed the revolution or qualified ray population until exact correlated evidence is classified as clear or qualified labelled occlusion. Qualified meaningful occluders persist their semantic labels and collision-aware scanning continues without automatic contact motion.
  fail_closed: [target re-detection failure, heavy worker failure or timeout, request or stamp mismatch, unsafe blocked invalid scene, unqualified occluder, executor capture wait timeout]
  preserved: [semantic thresholds, bootstrap ray behavior, capture format, model-seed hashing, qualified revolution and ray algorithm, authorization, Tesseract collision planning, runtime safety, terminal home disable cleanup]
  evidence: [273 focused workflow executor worker capability tests, 877 complete mobile tests, 165 complete Tesseract tests, both affected package builds, registered functional CTests, git diff check and Python compile, all 12 AI YAML files parse]
responsibility_package_bookmark_2026_08_27:
  status: structurally_moved_behavior_preserved
  owners: [mission/core.py, mission/engine.py, mission/spool.py, infrastructure/failure_model.py, infrastructure/telemetry_store.py, infrastructure/process_supervisor.py]
  compatibility_facades: [mission_core.py, mission_engine.py, mission_spool.py, failure_model.py, telemetry_store.py, process_supervisor.py]
  evidence: [333 focused mission infrastructure compatibility and external contract tests pass, facade exports are identity-tested, package owners remain ROS-free, internal imports remain acyclic, ROS interfaces configuration and runtime constants unchanged]
mission_resource_extraction_2026_08_27:
  status: structurally_extracted_behavior_preserved
  owner: mission/resources.py
  compatibility: target_scan_mission_node.py imports the same function objects for downstream callers while runtime calls use the owner directly.
  evidence: [five dedicated calibration dataset process and compatibility tests, 187 focused mission tests, 859 complete mobile tests with one skip, clean changed-file lint, focused package build]
execution_responsibility_package_2026_08_27:
  status: structurally_moved_behavior_preserved
  owners: [execution/authorization.py, execution/capture.py, execution/modes.py, execution/motion.py, execution/recovery.py, execution/trajectory.py, execution/validation.py]
  compatibility: Seven former root module paths explicitly export identical owner objects.
  evidence: [373 focused execution mission and external-contract tests, 860 complete mobile tests with one skip, facade identity and cycle tests, changed-file flake8 and pep257 pass]
planning_responsibility_package_2026_08_27:
  status: structurally_moved_behavior_preserved
  owners: [planning/capability.py, planning/coverage.py, planning/generation.py, planning/measured_surface.py, planning/ray_culls.py, planning/rays.py]
  compatibility: Six former paths explicitly export identical owner objects; white-box NBV monkeypatch tests now target the owner module.
  evidence: [161 focused planning tests with one existing replay skip, 861 complete mobile tests with one skip, facade identity and cycle tests, changed-file lint]
perception_responsibility_package_2026_08_27:
  status: structurally_moved_behavior_preserved
  owners: [perception/acquisition.py, perception/target_envelope.py, perception/landmark_geometry.py, perception/obstacle_geometry.py, perception/occlusion.py]
  compatibility: Five former paths explicitly export identical owner objects; obstacle and occlusion label normalization remain intentionally separate policies.
  evidence: [199 focused perception capture planner and session tests, 862 complete mobile tests with one skip, facade identity and cycle tests, changed-file lint]
tesseract_boundary_cleanup_2026_08_27:
  status: structurally_extracted_behavior_preserved
  owners: [piper_tesseract_foxy/candidate_selection.py, piper_tesseract_foxy/protocol/contract.py, piper_tesseract_foxy/protocol/spool.py]
  compatibility: piper_tesseract_foxy/contract.py explicitly re-exports identical protocol objects; bridge_node retains identical candidate helper attributes.
  evidence: [163 complete Tesseract tests, compatibility identity regressions, ROS-free architecture checks, changed-file lint, three-package dependency build]
gui_ros_adapter_cleanup_2026_08_27:
  status: structurally_extracted_behavior_preserved
  owner: piper_gui/ros_node.py
  compatibility: piper_gui_native.PiperGuiRos is the identical class object.
  evidence: [106 GUI and reconstruction-control tests, strengthened two-module production-source characterization, changed-file lint and compile]
reconstruction_input_cleanup_2026_08_27:
  status: structurally_extracted_behavior_preserved
  owner: reconstruction/input_provenance.py
  compatibility: tsdf_reconstruct re-exports identical input-admission constants and functions.
  evidence: [15 moved function ASTs identical to rollback commit, 54 reconstruction tests, direct out-of-tree CLI smoke, changed-file flake8 pep257 and compile]
driver_joint_policy_cleanup_2026_08_27:
  status: structurally_extracted_behavior_preserved
  owner: piper/piper/joint_state_policy.py
  compatibility: piper_ctrl_single_node retains identical function objects and equal constants.
  evidence: [nine moved function ASTs identical, 68 driver functional tests, piper_description and piper builds, targeted lint pep257 and compile, no CAN access]
capture_one_bound_model_seed_2026_08_26:
  status: physically_retested_generation_one_and_three_captures_passed
  capability: The first accepted model is derived from capture one's exact persisted semantic mask confidence-qualified target depth CameraInfo and recorded base-from-camera transform. Shape and transform are one hash-bound private seed persisted in frame metadata, returned by scan capture, admitted before workflow acceptance and promoted in accepted scan history. The planner builds the revolution directly from the recorded matrix, so TF cache lifetime cannot empty generation one.
  fail_closed: Missing malformed unsynchronized frame-mismatched nonhomogeneous or digest-invalid seeds abort before accepted history. A plain legacy saved response cannot authorize the first model.
  preserved: [post-settle final aim and border gate, transactional RGB-D capture, public ROS schemas, bootstrap-to-qualified population boundary, object centre and envelope geometry, ray and NBV behavior, Tesseract and executor gates, terminal home disable cleanup]
  evidence: [physical task codex-e2e-qualified-rays-20260826-2331 isolated the delayed-TF failure and safely shut down, 264 focused tests pass, 848 complete mobile tests pass, 162 complete Tesseract tests pass, changed-file lint mobile package symlink build and all 12 AI YAML parses pass, archived view_000 replay uses exact mask stamp and builds a valid thirteen-box envelope without TF, task codex-e2e-model-seed-20260827-0123 persisted model seed b97bdd2d5b530d3bc83085d3d9226cc5635618608e6268c229c46f1b510dc4f0 generated 353 qualified generation-one rays accepted three captures and safely cancelled]
object_sized_coverage_and_monotonic_ray_frontier_2026_08_26:
  status: physically_exercised_three_capture_partial_completion
  capability: The visible-surface lock owns only a temporary bootstrap population. Accepted capture one qualifies the mask-revolution axis as both grey-model and permanent-ray centre, produces a new population identity and resets bootstrap culls. The qualified identity remains stable across later standoff changes; its permanent culls remain absent from ranking, accepted captures retire an inclusive 15-degree neighbourhood, and path-dependent failures remain retryable. Ray Review shows the population reset, retains past culls as dashed audit rays by default, omits the pre-cull camera shell and shows L515 meshes only for post-cull active survivors.
  endpoint_boundary: Typed exhaustive RAY_IK_FAILURE is bridge-owned permanent endpoint evidence. Collision path visibility timeout transport generic planning and framing rejection remain retryable. The source-hashed worker and committed capability artifact are unchanged.
  evidence: [offline-cube-qualified-centre-replay-20260826 rebuilds two recorded captures at 0.03988083 m grey diameter, qualified ray and grey centres match exactly, bootstrap and qualified population SHA differ intentionally, later qualified SHA remains stable, bootstrap and old-centre IK culls are not reused, no repeated-new-cull or same-population resurrection occurs, 262 focused tests pass, complete 844-mobile 162-Tesseract and 95-GUI/root-ray suites pass, changed-file lint compile two-package build and all AI YAML validation pass]
  pending: [uninterrupted automatic completion and diversity-floor validation, motion-stream scheduler-load diagnosis]
settled_aimed_model_seed_order_2026_08_26:
  status: physical_order_and_generation_one_verified
  capability: Generation-zero bootstrap rays remain frozen from the visible-surface tracking lock without consuming live shape evidence. After the first ray settles and achieved aim passes, the executor uses a complete post-settle silhouette only for border admission. Capture one rebuilds the authoritative shape from its exact persisted mask/depth and binds its recorded transform; accepted capture one promotes that seed and rejection discards it. The planner revolves only that validated history seed, records the old lock as bootstrap provenance and creates the permanent population around the resulting axis origin.
  bounds: [same session only, no rough framing or cropped contour revolution, no pre-capture model seed, no planner live-shape subscription, no delayed TF for new seed, accepted-capture-only seed promotion, rejected pending seed discarded, no public interface change]
  evidence: [task codex-e2e-first-ray-20260826-1824 exposed the race and completed safe shutdown, 189 focused border and order tests pass, 835 complete mobile tests pass, 161 complete Tesseract tests pass, affected lint and mobile package build pass, task codex-e2e-model-seed-20260827-0123 proved capture-bound promotion and accepted three views]
first_scan_ray_target_framing_gate_2026_08_26:
  status: software_implemented_physical_retest_pending
  capability: Production missions treat ACQUIRED as tracking-only, reach and settle on the first selected scan ray, prove <=5-degree endpoint aim, then inspect a fresh silhouette immediately before capture. A middle-band crop captures nothing and retries the same ray with a larger minimum standoff. Endpoint aim always targets the frozen cube centre while Tesseract may use any safe path. No farther aimed endpoint is too close and a crop at 0.800 m is too large.
  preserved: [clipped masks never create envelopes, public ROS interfaces, stable measured target, 0.250 m surface clearance, 0.800 m maximum, Tesseract exact feasibility, executor runtime safety, terminal home disable cleanup]
  evidence: [163 focused tests pass, 835 complete mobile tests pass, 161 complete Tesseract tests pass, changed-file flake8 and pep257 pass, mobile package symlink build passes, all 35 registered functional targets pass, registered package red only on historical package-wide lint, task codex-e2e-achieved-lock-20260826-1746 physically established the phase error and safe shutdown]
azimuth_completion_gate_removed_2026_08_26:
  status: software_implemented_functional_suites_pass_physical_retest_pending
  capability: Automatic completion and next-view ordering no longer require or pursue a fixed 120-degree azimuth span. The measured span remains present in coverage payloads and Ray Review evidence as a diagnostic.
  remaining_completion_evidence: [8 through 24 accepted persisted captures, two achieved views per Y side, 25-degree elevation span, three-view measured masked-surface convergence]
  preserved: [accepted-only history, duplicate-direction floor now inclusive at 15 degrees, NBV information scoring, Tesseract exact feasibility, collision, visibility, GOOD/CLEAR capture, 24-view maximum, home, disable, shutdown]
  evidence: [19 focused scan-session tests pass, 179 mission and scan-motion tests pass, 815 complete mobile tests pass, 161 complete Tesseract tests pass, command-free 60-degree-span completion decision returns COMPLETE, changed Python files pass ament_flake8 and ament_pep257, 12 AI YAMLs parse, no hardware command]
ray_review_target_envelope_layers_2026_08_26:
  status: software_implemented_command_free_validated
  capability: Ray Review has independent default-on Estimated revolved model, Conservative planning boxes and Original mask/depth outline controls. New frozen envelopes preserve the original qualified contour as optional hash-bound base_link evidence; older reports remain readable without inventing an outline.
  authority: The smooth mesh is presentation-only, boxes remain planning evidence, and the ROS-free viewer cannot change any mission or motion output.
  evidence: [107 focused envelope bridge diagnostic and viewer tests pass, 975 complete mobile and Tesseract tests pass, 92 complete GUI tests pass, offscreen independent-toggle smoke passes, command-free 360-ray report carries outline evidence, two-package build passes, no hardware command]
clipped_target_shape_failure_2026_08_26:
  status: software_qos_repaired_physical_classification_pending
  capability: A target component touching any image edge publishes an exact-stamp hash-bound rejection instead of an undersized envelope. The executor makes it authoritative only after the first scan ray is settled and aimed, then applies the centre-distance 0.250 m too-close, same-ray farther retry and 0.800 m too-large contract.
  preserved: [valid complete-silhouette envelope geometry, 0.28 through 0.80 m ray bounds, 0.250 m surface clearance, NBV, Tesseract exact feasibility, motion, home, disable, cleanup, ROS action schema]
  evidence: [180 focused tests pass, 991 complete mobile and Tesseract tests pass, command-free target-shape DDS delivery and piper_mobile_manipulation symlink build pass, first five-percent framing attempt proved safe staged shutdown and exposed the corrected QoS handoff]
trusted_size_aware_target_envelope_2026_08_25:
  status: software_implemented_physical_qualification_pending
  capability: A qualified silhouette becomes a mission-frozen conservative target envelope that raises or rejects unsafe ray standoffs, retains every ray in schema-v2 review evidence, and binds the same collision boxes into Tesseract requests.
  evidence: [792 mobile tests, 160 Tesseract tests, 22 GUI and ray tests, successful two-package build, command-free 360-ray large-target simulation]
target_relative_capability_bounded_rays_2026_08_25:
  status: software_implemented_functional_suites_pass_changed_file_lint_and_physical_trace_pending
  capability: ray_nbv preserves the configured target-relative 0.28 through 0.80 m request regardless of target distance from base_link. Enforcing capability lookup records per-standoff support and contiguous runs, preserves requested bounds, narrows the active worker envelope to first through last support and seeds from a supported run.
  authority: [viewpoint_rays.py configured request and supported-run seed, capability_map.py sampled support, viewpoint_reachability_filter_node.py active-envelope narrowing, bridge_node.py private evidence handoff, worker.py exact endpoint feasibility]
  preserved: [coarse workspace authority, sparse-map prequalification-only meaning, NBV score and survivor order, six-ray shortlist size, accepted-only coverage, worker exact IK collision path aim and visibility checks, executor safety, motion, capture, home, shutdown, public ROS interfaces]
  evidence: [51 focused tests pass, 782 complete mobile tests pass, 158 complete Tesseract tests pass, 80 complete GUI tests pass, two-package symlink build passes, all 33 registered mobile functional targets pass, Tesseract registered 158-test suite passes]
  qualification: Seven changed-file flake8 and eight changed-file pep257 findings remain; one command-free live-shaped trace and later separately authorized physical evidence are pending.
mission_ray_diagnostic_report_2026_08_25:
  status: software_implemented_functional_tests_pass_changed_file_lint_and_physical_mission_artifact_pending
  capability: Every ray-NBV mission writes canonical JSON and an offline interactive HTML graphic covering the complete frozen pool, planner and accepted-history culls, information rank and metrics, workspace/capability culls, bounded Tesseract shortlist, exact worker failure evidence and selected ray for every accepted-view generation. The GUI opens these reports; the 3D view replays the URDF-derived PiPER chain and an L515 camera on every ray using only achieved or planned joint evidence. Reacquisition sessions remain grouped by mission task identity.
  output: datasets/ray_diagnostics/<mission_id-or-session_id>/RayProcesses - HH:MM - DD-MM-YYYY.json plus exact coverage snapshots and same-stem compatibility HTML; historical datasets/active_scan/ray_diagnostics reports and legacy ray_mission_diagnostics files remain readable
  viewer: Open Ray Review reuses one ROS-free stdin-driven PyQt5/VTK child with synchronized Mission Process and read-only Capability Map tabs
  authority: ray_mission_diagnostics.py observes scan_viewpoint_planner_node.py, viewpoint_reachability_filter_node.py and piper_tesseract_foxy/bridge_node.py decisions
  preserved: [NBV ranking and gain floor, capability map, six-ray shortlist, schema-v5 Tesseract bytes and hashes, exact feasibility, motion, capture, mission safety, home and shutdown]
  historical_replay: 35 completed datasets and 116 accepted captures replayed command-free with zero skipped captures; archives do not preserve the complete historical rejected-ray population and are labelled partial.
  evidence: [51 focused ray capability diagnostic hard-cull and GUI tests pass, 782 complete mobile tests pass, 158 complete Tesseract tests pass, 80 complete GUI tests pass, two-package symlink build passes, all 33 registered mobile functional targets pass, Tesseract registered 158-test suite passes, all AI YAML parses, changed-file lint remains pending]
persistent_static_ray_culls_2026_08_25:
  status: software_implemented_functional_suites_pass_changed_file_lint_pending
  capability: Population-hash-bound complete snapshots carry only static workspace/capability-map rejection or worker-proven permanent endpoint failure back to the planner, which removes matching rays before later history and NBV ranking instead of regenerating and rescoring them every accepted-view generation.
  interface: Private reliable transient-local std_msgs/String /piper/ray_hard_culls with two enumerated source publishers and one planner consumer.
  preserved: [complete immutable generated pool, accepted-only coverage, relative NBV rank of survivors, contextual Tesseract failure lifetime, six-ray shortlist, exact worker request and response hashes, motion, capture, safety, home and shutdown]
  evidence: [51 focused ray capability diagnostic hard-cull and GUI tests pass, 782 complete mobile tests pass, 158 complete Tesseract tests pass, 80 complete GUI tests pass, two-package symlink build passes, all 33 registered mobile functional targets pass, Tesseract registered 158-test suite passes, AI YAML parse passes]
  qualification: Changed-file flake8 and pep257 still fail, mobile registered colcon remains red on package-wide lint, and a new physical mission artifact is pending; do not promote this entry to software-qualified yet.
generation_zero_nearest_ray_seed_2026_08_25:
  capability: The first ray_nbv request orders the existing six-ray prequalified shortlist by minimum physical camera travel to each continuous bounded interval. It no longer treats legacy negative-Y preference as measured information before any capture exists.
  authority: [bridge_node.py generation-zero shortlist ordering, latest achieved FK physical start, Tesseract exact endpoint feasibility]
  preserved: [frozen ray pool, capability and analytic culling, six-ray cap, accepted-only coverage, post-capture NBV scoring and ranking, motion, capture, home, shutdown, public ROS interfaces]
  evidence: [focused bridge regressions, no hardware command]
startup_wrist_terminal_prerequisite_2026_08_24:
  status: software_qualified_physical_retest_pending
  capability: A reboot-time ambiguous first J6 observation can no longer permanently reject an otherwise valid explicit negative-branch STARTUP_WRIST. Terminal recovery cannot enter six-joint PRE_HOME until that positive-only J6 prerequisite or the complete rough startup home has been proved.
  authority: [piper_ctrl_single_node.py explicit re-arm and direction watchdog, mission_engine.py terminal stage ordering, target_scan_mission_node.py existing direct-stage adapter]
  preserved: [positive-only startup direction, J6 range and controller mapping, measured J1-J5 preservation, configured targets, tolerance, speed, motor-loss no-motion path, direct-home Tesseract bypass, public ROS interfaces]
  evidence: [146 focused driver mission and shutdown tests pass, 834 complete behavior tests pass, five-package build passes, all registered functional colcon targets pass, no hardware command]
camera_capability_map_2026_08_24:
  feature: Deterministic collision-aware sparse camera capability atlas used as an exchangeable target-ray prequalification layer before the unchanged Tesseract bridge.
  authority: [capability_map.py, viewpoint_reachability_filter_node.py, capability_map_generator.py, generate_capability_map.sh, validate_capability_map.py, piper_camera_capability_map.npz, capability_map_convergence.json]
  proven: [2M deterministic samples, zero malformed states, 1479561 occupied 5D bins, all 87 recorded achieved poses supported, practical convergence report, source-hash rejection, explicit non-geometric manifest-hash rebind provenance, floor-profile lookup, coarse fallback, target-ray-only enforcement, per-standoff support and disjoint-run retention, active-envelope narrowing with requested-bound preservation, 51 current focused ray and diagnostic tests]
  not_changed: [NBV generation scoring ranking and coverage, bridge six-ray shortlist, worker IK collision path aim and visibility, motion speed, capture, home, disable, shutdown, ROS schemas]
  physical_status: The tabletop collision profile is qualified for plans that individually pass all existing safety gates. The tracked-ground profile remains explicitly unqualified for physical motion.
tracked_robot_drop_in_description_2026_08_24:
  implemented: [deterministic tracked-root description generator, ready two-file replacement bundle, geometry-free piper_base_link gateway alias, exact current arm-subtree synchronization, duplicate Bunker exclusion, pinned source and output hashes, installation guide]
  proven: [single robot_bottom root, tracked base and sensor frames preserved, PiPER camera chain present, current J6 pi limits retained, friend description contract 18 tests pass, odom gateway frame matches existing default]
  boundary: No ROS interface, gateway behavior, planner, motion, calibration or hardware command changed.
bunker_collision_profile_2026_08_24:
  feature: Permanent combined PiPER/L515/Bunker visual and collision model with an idle-GUI-selectable next-mission support-floor height.
  authority: [piper_description.xacro, collision_environment.yaml, collision_environment.py, piper_gui_native.py, collision_model.yaml, collision_model_ground.yaml, piper_bunker.srdf, floor_profile.sh, platform_sources/PROVENANCE.md, platform_planning_150mm/collision_mesh_manifest.json, collision_meshes.py, model_builder.py]
  proven: [exact external source hashes, deterministic transforms and bounds, every source triangle retained, visual-only platform links become decomposed planning collision links, atomic GUI floor persistence, per-mission snapshot, model load, positive neutral clearance, mixed worker/bridge model blocks readiness]
  not_yet_proven: [generic core acquisition qualification, motors-disabled visual inspection, physical collision accuracy, supervised motion]
deadline_terminal_home_authorization_2026_08_24:
  status: software_qualified_physical_retest_pending
  capability: Deadline expiry terminates scanning but no longer consumes the authorization needed by terminal recovery. After scan stop/hold, or fresh static-startup feedback, each existing direct-home stage receives a bounded same-identity authorization window and the normal staged home, disable and cleanup sequence continues.
  authority: [mission_engine.py terminal order, target_scan_mission_node.py bounded renewal, scan_viewpoint_executor_node.py unchanged stage validation]
  preserved: [public ROS interfaces, deadline value, home targets and tolerance, motion speed, collision checks, motor-loss no-command path, NBV and Tesseract behavior]
  evidence: [57 mission-core tests, 356 focused shutdown and safety tests, 748 complete mobile tests, no hardware command]
installed_l515_collision_assembly_2026_08_24:
  status: software_qualified_physical_retest_pending
  capability: The user-aligned camera_holder and L515 visual geometry is automatically assembled in gripper_base coordinates and represented in Tesseract by seventeen hash-verified overlapping 30 mm XY collision cells. Conservative cable/mount padding and the independent support-plane check remain.
  authority: [piper_description.xacro, l515_attached_assembly.STL, planning_30mm/collision_mesh_manifest.json, collision_meshes.py, collision_model.yaml, piper.srdf]
  preserved: [original RViz visuals, hand-eye and optical TF authority, robot motion, home, shutdown, speed, limits, public ROS interfaces]
  evidence: [4 generator tests, 11 description tests, 141 Tesseract tests, two-package build, installed-asset comparison, command-free core and compact qualification]
ray_endpoint_feasibility_cache_2026_08_23:
  status: software_qualified_physical_retest_pending
  capability: Every shortlisted direction is represented by one bounded standoff interval. The worker jointly solves six joints plus continuous standoff using nine deterministic multistart seeds, exhausts exact target aim before the bounded fallback, and returns the actual chosen endpoint. Local solver failures remain accepted-generation contextual rather than claiming complete-ray global infeasibility.
  diversity: Accepted directions within ten degrees are removed before NBV scoring handoff; global cumulative scoring still runs after every accepted capture.
  authority: [scan_viewpoint_planner_node.py and nbv_coverage.py information rank, viewpoint_rays.py interval binding, bridge_node.py cache and six-ray handoff, worker.py interval endpoint and path feasibility, contract.py interval response validation, executor and scan_capture_node.py achieved pose and acceptance]
  evidence: [196 focused tests pass, 747 complete mobile tests pass, 148 complete Tesseract tests pass, five-package build passes, command-free rootless core and compact qualifications PASS, registered functional tests pass, changed files pass focused flake8 and pep257]
updated_at: 2026-08-25
shared_settled_capture_deadline_2026_08_22:
  status: software_implemented_physical_retest_deferred
  capability: The command-free recorder still requires exactly 20 new synchronized settled native frames, but now consumes 18 seconds of the executor's existing 20-second capture_timeout_sec and reserves two seconds for returning the Trigger result. Timeout diagnostics expose count, elapsed time and synchronized rate.
  incident: gui-sim-8733327f74f3455eb8656f01a6ceb66d failed only after a later burst reached 18/20 at the former five-second limit; its first burst completed in 1.031 seconds and terminal staged shutdown was safe.
  preserved: [20-frame requirement, temporal median and confidence thresholds, capture acceptance, retry semantics, NBV, Tesseract, motion, home, disable, cleanup, public ROS interfaces]
offline_resegmented_reconstruction_masks_2026_08_22:
  status: software_implemented_dataset_replay_improved_but_quality_failed
  capability: Reconstruction Validation defaults to the trusted captured mask and exposes offline_resegment explicitly. That optional mode runs one fresh GroundingDINO and SAM2 image pass per immutable RGB, rejects live-mask fallback, stores hash-bound derived masks and comparison overlays, and intersects only with existing confidence-qualified target support. Dataset selection loads existing raw/cleaned outputs for inspection even when their quality is FAIL or a later optional resegmentation attempt fails.
  evidence: [44 focused reconstruction and GUI tests pass, 45 perception and native-GUI tests pass, seven fresh masks generated from scan_20260821_220003, view-004 shadow support reduced from 7342 to 4976 pixels, auto mesh OBB improved to 51.48 x 49.00 x 45.77 mm but remains FAIL]
  preserved: [immutable capture artifacts, live SAM2 audit mask, captured depth authority, registration choices and bounds, reconstruction quality gates, public ROS interfaces, tracking, NBV, Tesseract, motion, home and shutdown]
selectable_static_scene_pose_graph_reconstruction_2026_08_22:
  status: software_implemented_dataset_replay_failed_quality
  capability: The offline GUI and CLI can select robot_pose or scene_pose_graph. The new path registers target-excluded aligned static-scene RGB-D using robot-seeded odometry and bounded GICP, while the target TSDF remains confidence-qualified and target-only.
  evidence: [39 focused reconstruction and GUI-support tests pass, seven-view robot_pose and scene_pose_graph replays complete, auto evaluates and safely rejects the less coherent scene candidate]
  preserved: [immutable capture history, robot-pose baseline, 20 mm and 5 degree correction bounds, existing quality gates, mesh voxel selector, public ROS interfaces, NBV, Tesseract, motion, home and shutdown]
bounded_multiway_reconstruction_2026_08_21:
  status: software_implemented_recalibration_pending
  capability: Offline TSDF can compare robot poses, sequential bounded GICP and a connected bounded multi-view pose graph, then auto-select only a materially better structural candidate. All residuals remain within 20 mm and 5 degrees and are fully reported.
  calibration_support: Fixed-board validation now uses the exact image timestamp for TF; collection and deployment remain operator-controlled.
  evidence: [36 reconstruction and GUI-support tests pass, 7 fixed-board tests pass, scan_20260821_220003 improved to 90.5 percent dominant component and 2.25 mm p90 residual but correctly remains FAIL against the 35 mm cube]
  preserved: [immutable captures, robot-pose baseline, public ROS interfaces, capture and NBV, Tesseract, motion, home, shutdown, calibration deployment authority]
gui_selectable_mesh_density_2026_08_21:
  capability: Manual command-free reconstruction exposes a 0.5-through-3.0 mm TSDF voxel slider, defaults to the restored 3.0 mm field-usable fusion value, passes the selected resolution through the existing CLI and displays actual vertices/triangles plus unchanged quality evidence. The worker default truncation band is restored to 15 mm.
  boundary: It does not change input capture density, GICP resolution, automatic mesh jobs, registration acceptance or any robot subsystem.
full_resolution_measured_cloud_viewer_2026_08_27:
  capability: Every reconstruction persists all accepted temporally averaged target-depth points after the selected refined base_link transform as a capture-colored PLY with no display downsampling. Reconstruction Validation can open that cloud directly or overlay it on the raw or cleaned mesh, including for FAIL-quality reports.
  provenance: Quality JSON records measured_cloud_path measured_cloud_sha256 measured_point_count and per-view point counts/colors. Colors identify capture index only.
  boundary: Diagnostic export and presentation only; capture evidence, registration, TSDF, cleanup, quality gates, ROS, safety and motion are unchanged.
partial_safe_frontier_reconstruction_2026_08_21:
  status: software_implemented_current_dataset_diagnostic_fail
  capability: Deferred and command-free GUI reconstruction admit 1-24 immutable captures without a fixed eight-view admission or PASS floor. Mesh generation and durable success are separate; only PASS or WARN succeeds, while FAIL preserves diagnostic outputs and fails the durable job.
  provenance: Future missions bind the exact deployed hand-eye calibration file SHA-256. Historical missing-identity scans remain DIAGNOSTIC_ONLY and are never rewritten.
  environment: reconstruction/.venv is shared by installer GUI and gateway.
  evidence: [scan_20260821_185143 three-view gateway replay, actual GUI Build and Validate plus Open 3D Viewer replay, 615 vertices, 719 triangles, overall FAIL correctly retained, 30 reconstruction tests, 47 mission/job tests, 726 mobile tests, package build]
supervised_ray_nbv_e2e_2026_08_21:
  status: one_supervised_e2e_passed
  capability: Frozen full-sphere ray NBV now advances across bounded failed shortlists, classifies an internal budget as ray exhaustion only after an actual attempted ray, and can terminate adaptively after at least one accepted achieved capture when the entire remaining prequalified safe frontier is genuinely exhausted.
  evidence: codex-e2e-20260821-185113-adaptive accepted three views, returned SUCCEEDED with safe_shutdown true, and left only persistent GUI RViz display and coordinator nodes.
  preserved: [accepted-only coverage, executor achieved FK, exact-first aim, Tesseract feasibility, GOOD/CLEAR capture gate, speeds, staged home, disable, owned-process cleanup, public ROS interfaces]
ray_workspace_intersection_cull_2026_08_21:
  status: software_implemented_physical_qualification_pending
  capability: Target rays whose entire bounded standoff interval lies outside the configured coarse camera workspace are rejected before bridge/Tesseract shortlisting. A ray survives if any permitted point intersects.
  authority: viewpoint_reachability_filter_node.py
  preserved: [exact-point policies, NBV scoring and ranking, accepted-only coverage, Tesseract IK collision path visibility, motion, safety, capture, home, shutdown, public ROS interfaces]
  evidence: [108 focused tests, 716 complete mobile tests, 125 complete Tesseract tests, five-package build, changed-file ament lint, AI YAML parse]
exchangeable_nbv_policy_boundary_2026_08_21:
  status: software_implemented_physical_qualification_pending
  capability: A private capability table binds legacy, voxel and ray policies to the correct candidate geometry and behavior. Active policy is frozen at stack startup; GUI changes apply to the next mission. Authoritative NBV requires 0.02 normalized predicted marginal information. voxel_nbv keeps twelve exact points; ray_nbv tries at most six bounded intervals and the worker solves each continuously over joints and standoff.
  authority: [view_generation.py, nbv_coverage.py, scan_viewpoint_planner_node.py, viewpoint_rays.py, piper_tesseract_foxy bridge contract and worker]
  preserved: [legacy and shadow, seed generations, accepted-only coverage, exact-first five-degree aim, Tesseract feasibility, motion, capture, safety, home, disable, shutdown, public ROS interfaces]
  evidence: [117 focused tests, 711 complete mobile tests, 125 complete Tesseract tests, 112 root tests with one hardware skip, 32 isolated AI tests, five-package build, changed-file ament lint, AI YAML parse]
frozen_viewpoint_ray_pool_2026_08_21:
  status: one_supervised_e2e_passed_repeatability_pending
  capability: Selectable ray_nbv freezes a configurable target-sector, upper-hemisphere or full-sphere direction pool per mission, keeps each configured target-relative 0.28 through 0.80 m request independent of target base-link range, ranks from accepted-only coverage, rejects normalized predicted gain below 0.02, shortlists at most six directions, and binds each selected direction to its prequalified active Tesseract envelope plus supported-run evidence when available. voxel_nbv remains selectable as the exact-point lattice with its twelve-point handoff for performance comparison.
  defaults: {policy: ray_nbv, ray_region: full_sphere, ray_count: 175}
  authority: [scan_viewpoint_planner_node.py, viewpoint_rays.py, piper_tesseract_foxy/bridge_node.py]
  retained_boundaries: {target_estimate: target_tracker_node.py, coverage: nbv_coverage.py, feasibility: worker.py, achieved_pose: scan_viewpoint_executor_node.py, capture_acceptance: scan_capture_node.py}
  software_evidence: [96 focused tests, 818 complete mobile plus Tesseract tests, 100 repository tests with one hardware skip, five-package build, rootless core and compact Tesseract PASS, changed-file lint, AI YAML parse]
target_too_far_terminal_result_2026_08_21:
  status: software_implemented
  capability: Exact-job semantic masked depth entirely beyond 1.20 m produces TARGET_TOO_FAR / REPOSITION_REQUIRED with rough base_link XYZ and normal shutdown. Five-look absence also reports XYZ while retaining TARGET_NOT_FOUND.
  authority: [heavy_refresh_bridge_node.py, scan_execution_modes.py, scan_viewpoint_executor_node.py, mission_engine.py, target_scan_mission_node.py]
normalized_marginal_nbv_diversity_2026_08_20:
  status: software_implemented_supervised_physical_retest_pending
  capability: Global voxel NBV compares the fraction of projected target support that is cumulatively new, then accepted-view angular novelty, absolute marginal support and travel. Raw unknown projection can no longer force every rank into one steep elevation band.
  authority: [nbv_coverage.py, scan_viewpoint_planner_node.py]
  provenance_path: [scan_viewpoints, reachability filter, Tesseract request and response, tesseract_plan_provenance, frame metadata]
  preserved: [accepted-capture-only coverage, complete configured candidate region, six-degree duplicate floor, twelve-exact-point voxel handoff, six-direction ray handoff, Tesseract feasibility, exact-first five-degree aim, tracking, capture thresholds, motion, home, disable and shutdown]
  evidence: [118 focused tests, 685 complete mobile tests, 117 complete Tesseract tests, five-package build, rootless core and compact PASS, changed-file lint, offline replay of scan_20260820_230845 shifts top twelve from all 75-degree to 15 and 25-degree candidates spanning both Y extremes]
live_tracker_cadence_and_nbv_shortlist_handoff_2026_08_20:
  status: software_implemented_mid_elevation_physical_rerun_pending
  capabilities: [five-second bounded stationary prediction across measured bursty GPU gaps, process noise 0.01, base-noise Mahalanobis gate independent of confidence scaling, six broad-sector global NBV leaders, five mid-elevation sector alternatives, one nearby informative continuity fallback inside the existing twelve-candidate shortlist, final Tesseract attempt order by NBV rank]
  authority: [target_tracker_node.py, tracking_params.yaml, piper_tesseract_foxy/bridge_node.py]
  preserved: [NBV generation, information gain, cumulative coverage, candidate ranks, Tesseract feasibility, exact-first five-degree aim, motion, capture, home and shutdown]
  evidence: [task codex-e2e-tracking-20260820-2106 proved startup seed capture and safe shutdown, task codex-e2e-clear-20260820-223207 committed three true-NBV captures and proved safe shutdown, live-shaped replay retains exact-feasible mid-elevation views on both sides of the reachable arc, focused bridge tests and rootless exact-aim IK pass]
target_measurement_validity_and_prediction_only_2026_08_20:
  status: software_implemented_partial_physical_loop_observed
  capabilities: [fresh same-frame nonempty target mask required for Target3D, no unmasked ROI fallback, finite timestamped base_link measurements, confidence-scaled correction noise, base-noise Mahalanobis innovation rejection, near-static zero-velocity model with process noise 0.01, bounded prediction-only LOW_CONFIDENCE output, five-second LOST timeout and reset]
  authority: [depth_to_3d_node.py, target_tracker_node.py, utils/kalman_filter.py, tracking_params.yaml]
  preserved: [one authoritative target estimate, existing TrackedTarget and target status interfaces, NBV behavior, Tesseract, accepted coverage, capture, motion, home and shutdown]
  software_evidence: [60 focused tests pass, 680 mobile functional tests pass, 115 unchanged Tesseract tests pass, 120 repository tests pass with one hardware-dependent skip, 11 isolated AI worker tests pass, five-package build passes, changed-file lint passes]
  deferred: The 2026-08-23 repair removed eight-frame prediction-to-conditioning reseeding and motion-prompt seed authority while preserving bounded history. CUDA replay and supervised display-only validation remain; only persistent drift after that evidence should trigger a separate detector-mask investigation.
global_nbv_target_aim_capture_recovery_2026_08_20:
  status: implemented_partial_physical_loop_verified_complete_scan_pending
  capabilities:
    - Cumulative accepted voxel coverage ranks the complete configured scan region; the retired 6-to-30 and 30-to-45 degree movement frontiers have no production authority.
    - Information gain outranks motion cost, a six-degree accepted-view threshold rejects redundancy, authoritative predicted marginal information below 0.02 is rejected, and only twelve voxel exact points or six ray directions reach Tesseract.
    - Exact current-target aim is tried first through roll and IK alternatives. At most one canonical fallback within five degrees is allowed and provenance is validated.
    - Every settled commanded viewpoint stores actual FK independently of capture acceptance. Rejected views change next motion start but not accepted coverage or reconstruction.
    - One request-correlated heavy refresh handles transient fresh visual capture rejection before a persistent rejection requests a new NBV.
    - Qualified component depth produces coherent camera X/Y/Z, temporal filtering happens only after timestamped transformation into base_link, and immutable accepted reconstruction remains separate from current target estimation.
  replay: scan_20260820_142919 old 13.424-degree final target error is detected and rejected by the five-degree gate; selected-to-achieved error was only 0.037 degrees.
  authority: [depth_to_3d_node.py, target_tracker_node.py, nbv_coverage.py, scan_viewpoint_planner_node.py, viewpoint_reachability_filter_node.py, piper_tesseract_foxy/bridge_node.py, piper_tesseract_foxy/contract.py, piper_tesseract_foxy/worker.py, scan_viewpoint_executor_node.py, capture_coordinator.py, scan_capture_node.py]
  software_evidence: [669 mobile functional tests pass, 115 Tesseract tests pass, affected ROS packages build, rootless Tesseract core and compact qualifications pass, AI YAML parsing passes]
  physical_evidence: [seed capture committed, true voxel_nbv generation one selected view 164 with predicted_unknown 898 and novel_surface 50, strict 5.037-degree final-aim rejection preserved safety, command-free post-view reacquisition route exercised, every terminal run proved staged home storage disable and exact child cleanup]
  remaining: Complete diverse scan is not yet qualified because the live target lock becomes unstable between views; estimator and tracking repair is explicitly deferred.
post_startup_full_logical_j6_range_2026_08_20:
  status: implemented_software_verified_physical_retest_pending
  capabilities:
    - Positive-only startup retains its raw +360-degree turn and never commands the unsafe raw-zero unwind.
    - The controller qualification covers the complete later logical [-pi,+pi] scan range through raw +540 degrees, with a 545-degree configured margin.
    - A post-startup positive logical J6 target is regression-tested, while an unqualified raw overflow is rejected before CAN transmission.
  authority: [piper_ctrl_single_node.py, test_enable_handshake.py, test_joint_feedback_continuity.py]
  software_evidence: [60 focused J6 tests pass, 62 full piper tests pass with one skip, 643 mobile functional tests pass, 106 Tesseract tests pass, five ROS packages build, rootless core and compact qualifications pass]
previous_generation_reconciliation_2026_08_20:
  status: implemented_software_verified_physical_lifecycle_retest_pending
  capabilities:
    - Operator-recovery releases exact non-command processing groups without stopping a possibly powered driver or scan executor.
    - A later admission automatically reaps processing-only exact survivors.
    - A prior driver generation is reaped only after fresh typed all-six-disabled proof.
    - Cleanup failure or unproved powered ownership remains fail-closed and starts no new process generation.
  authority: [mission_engine.py, target_scan_mission_node.py, process_supervisor.py]
  software_evidence: [748 mobile-manipulation and Tesseract tests pass, 127 repository regressions pass with one hardware skip, changed-file flake8 passes, five ROS packages build, rootless core and compact qualifications pass]
voxel_nbv_generation_synchronization_2026_08_17:
  status: implemented_software_verified_physical_retest_pending
  capabilities:
    - Exact session and accepted-capture generation follows every planner candidate set through the preliminary filter into the Tesseract bridge.
    - MissionEngine waits for a bridge-cached post-barrier generation instead of requesting against expired scan data.
    - First view is explicitly voxel_nbv_seed; later authoritative generations use measured schema-2 voxel evidence.
    - Bounded Tesseract shortlists reserve top information candidates from distinct azimuth/elevation bins and retain local IK alternatives.
    - Selected policy, generation, rank and predicted gain follow the worker response to a plan provenance topic and exact per-frame metadata.
  authority: [view_generation.py, nbv_coverage.py, scan_viewpoint_planner_node.py, viewpoint_reachability_filter_node.py, piper_tesseract_foxy/bridge_node.py, piper_tesseract_foxy/worker.py, mission_engine.py, target_scan_mission_node.py, scan_capture_node.py]
  software_evidence: [741 mobile-manipulation and Tesseract tests pass, 127 repository regression tests pass and one hardware-dependent test skips, 11 changed Python files pass ament_flake8, five ROS packages build, rootless Tesseract core and compact qualification pass]
purpose: >-
  Preserve the system capabilities that must not be forgotten during the
  core-integration rewrite. This is a feature inventory, not a declaration
  that the complete physical pipeline has passed end to end.

repository_snapshots:
  active_rollback:
    path: /home/prl/Piper_arm
    commit: 4945b480d8494fa840c0d2bc993c72834934a37f
    branch: rewrite/core-integration-v2
    meaning: Latest GitHub-pushed baseline restored to the canonical absolute path.
  preserved_later_version:
    path: /home/prl/Piper_arm_previous_version
    commit: a96117f5143896efef5b1a0199a6e2e45bcb24b3
    branch: archive/pre-core-rewrite-2026-08-14
    meaning: >-
      Complete pre-rewrite integration snapshot containing later features and
      fixes that are not all present in the active rollback.

status_terms:
  ACTIVE: Present in the active rollback source.
  ARCHIVED_LATER: Present only, or materially improved, in the preserved later version.
  PHYSICALLY_DEMONSTRATED: At least one bounded physical run exercised the stated behavior.
  SOFTWARE_QUALIFIED: Tests/builds or command-free qualification passed, but full physical acceptance is incomplete.
  PARTIAL: Interfaces or policy exist, but the intended real-world behavior is not complete.

active_rollback_features:
  - id: single_owner_gripper_diagnostic
    status: [ACTIVE, SOFTWARE_QUALIFIED]
    summary: >-
      A Python 3.8 direct-SDK tool provides receive-only status/monitor and
      exact-confirmation gripper-only disable-clear, homed-only
      measured-aperture clear-enable and homed-only motion diagnostics. Driver logging records
      ROS enable/disable requests, J1-J6 state, exact gripper command and fresh
      gripper enable/homing/fault feedback without changing control behavior.
    authority: [tools/debug_piper_gripper.py, tests/driver/test_debug_piper_gripper.py, piper_ctrl_single_node.py, test_enable_handshake.py]
  - id: always_on_mission_coordinator
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Singleton RunTargetScan action coordinator remains ready between jobs,
      owns mission process generations and returns durable task outcomes.
    authority: [run_target_scan_mission.sh, target_scan_mission_node.py, mission_core.py]

  - id: closest_first_multi_target_queue
    status: [ACTIVE, SOFTWARE_QUALIFIED]
    summary: >-
      Bounded eight-entry queue groups near-simultaneous jobs, selects the
      closest rough target first, preserves arrival order for ties and runs
      exactly one physical mission at a time through its shutdown boundary.
    authority: [target_scan_mission_node.py, mission_spool.py]

  - id: gui_tracked_robot_substitute
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      The Tk GUI can submit the same typed text-label plus stamped rough-pose
      mission contract intended for the tracked robot and exposes Cancel/Home.
      Commissioning motion starts without a command publisher and acquires one
      only after the graph proves no other writer. Unfinished startup J6
      retains positive-only direction authority, and the explicit no-home
      commissioning disable sends no motion before the driver's all-six proof.
    authority: [piper_gui_native.py, RunTargetScan.action]

  - id: managed_stack_lifecycle
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      The coordinator starts the disabled arm driver, L515, GPU perception,
      hand-eye TF, Tesseract and scan nodes in order, monitors critical
      children, and owns their cleanup while coordinator and RViz may remain.
    authority: [target_scan_mission_node.py, target_scan_mission.launch.py, run_gpu_vision_pipeline.sh]

  - id: typed_safety_and_control_authority
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Fresh joint feedback, all-six motor enable state, per-motor faults,
      controller limits, plan identity/hash and sole command-publisher
      ownership gate every powered motion. Partial enable or a motor fault
      revokes authority and triggers all-axis disable behavior.
    authority: [piper_ctrl_single_node.py, scan_viewpoint_executor_node.py, PiperStatusMsg.msg]

  - id: bounded_speed_execution
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Qualified low-speed execution scales the PiPER MoveJ model once, streams
      collision-qualified paths at 20 Hz, caps adjacent commands, monitors
      following error and proves final convergence and settling.
    authority: [scan_trajectory.py, scan_viewpoint_executor_node.py, scan_execution_params.yaml]

  - id: isolated_six_joint_tesseract_planning
    status: [ACTIVE, SOFTWARE_QUALIFIED]
    summary: >-
      A network/CAN/ROS-free Tesseract 0.35 worker performs free six-joint IK,
      deterministic OMPL/ISP planning, dense Bullet validation, response
      hashing and proposal-only qualification using the PiPER, gripper,
      camera-holder and conservative L515 geometry.
    authority: [motion_planning/tesseract, piper_tesseract_foxy]

  - id: close_range_one_look_acquisition
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Acquisition starts from the measured camera pose, tries a compact
      centered look and then bounded left/right/up/down looks near 15 degrees,
      performs one observation per plan and stops after a measured lock or at
      most five looks.
    authority: [scan_target_acquisition_node.py, target_acquisition.py, target_scan_mission_node.py]

  - id: open_vocabulary_grounded_segmentation
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Offline-cached GroundingDINO supplies language-conditioned detections,
      SAM2 supplies temporal target/obstacle masks, and request-correlated
      heavy refresh reseeds perception after settling or tracking loss.
    authority: [AI_perception_tests/groundingdino_test, heavy_refresh_bridge_node.py, sam2_live_bridge_node.py]

  - id: depth_tf_validated_target_lock
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Masked aligned depth, camera intrinsics, capture-time TF, tracking health,
      stable base-frame target state and LOCKED status are combined before a
      measured target may authorize scan planning.
    authority: [depth_to_3d_node.py, target_tracker_node.py, target_landmark_node.py]

  - id: occlusion_evidence_and_removal_proposals
    status: [ACTIVE, PARTIAL]
    summary: >-
      Target and obstacle masks are projected into base_link, occlusion quality
      is assessed after a correlated semantic probe, and the workflow can
      propose outward push or support-validated pick/place with an observed
      drop patch. Real manipulation remains unqualified.
    authority: [obstacle_instance_3d_node.py, occlusion_checker_node.py, supervised_workflow.py]

  - id: adaptive_closed_loop_nbv
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Automatic scanning plans one viewpoint at a time from the measured target,
      stores achieved camera FK and accepted/rejected history, replaces rejected
      views, and uses target-facing direction, Y-side, azimuth, elevation and
      measured-surface novelty rather than a fixed capture count alone.
    authority: [scan_viewpoint_planner_node.py, scan_session_memory.py, surface_coverage.py]

  - id: adaptive_capture_budget
    status: [ACTIVE, SOFTWARE_QUALIFIED]
    summary: >-
      Eight accepted views seed the model and 24 is the maximum. Completion
      requires useful achieved-view diversity and three consecutive low
      measured novel-surface gains; reaching the cap without those floors is a
      retryable quality failure.
    authority: [target_scan_mission_node.py, scan_session_memory.py, surface_coverage.py]

  - id: validated_rgbd_mask_capture
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Each accepted settled viewpoint writes immutable RGB, raw depth, metric
      depth PNG, mask and metadata with intrinsics, joints, camera transform and
      mission/plan identity. Capture one increments the accepted count only
      after synchronized GOOD target-valid persistence and exact semantic
      acceptance; later classified occluded views now use the same exact
      semantic qualification while CLEAR views accept directly.
    authority: [scan_capture_node.py, scan_capture.py, scan_quality_node.py]

  - id: tracking_loss_hold_and_replan
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      LOW_CONFIDENCE, LOST, SEARCHING, prediction-only or stale target state
      blocks capture and new motion, holds the achieved pose, requests semantic
      recovery and then revalidates or replans from current measured state.
    authority: [scan_viewpoint_executor_node.py, target_scan_mission_node.py]

  - id: staged_home_profile_and_cancel_path
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      Hash-verified schema-4 home data binds positive-only startup J6 toward the
      mission-ready angle, terminal-only pre-home, rough home, decreasing
      storage J6, retained final position-controller target, feedback-confirmed disable and owned-child
      cleanup. Configured home stages use ExecuteHomeStage/native MoveJ rather
      than the Tesseract worker and accept each joint inside the explicit
      operator-authorized 0.3-rad band for one second. The GUI exposes and
      preserves all three configured stages; supervised physical
      requalification remains required.
    authority: [piper_home_pose.json, home_pose.py, target_scan_mission_node.py]

  - id: single_authority_failure_shutdown_and_named_runtime_gates
    status: [ACTIVE, SOFTWARE_QUALIFIED]
    summary: >-
      MissionEngine is the only admitted mission/shutdown state machine. Every
      owned controllable failure first proves unfinished STARTUP_WRIST when
      required, then independently re-qualifies PRE_HOME, ROUGH_HOME and
      STORAGE_WRIST from current control evidence; only current
      motor authority loss forbids all commands. Autonomous startup and
      terminal shutdown do not call the redundant hold service. Executor runtime evidence is selected by one
      immutable named RuntimeGatePolicy rather than interacting booleans or a
      duplicate shadow evaluator. Supervised physical requalification remains
      required.
    authority: [mission_engine.py, mission_core.py, safety_evaluator.py, scan_viewpoint_executor_node.py]

  - id: calibrated_l515_tf_and_robot_model
    status: [ACTIVE, PHYSICALLY_DEMONSTRATED]
    summary: >-
      The installed camera holder and L515 visual model, manufacturer depth
      datum, serial-specific factory color/depth extrinsic and corrected 17 mm
      square/12 mm marker validation are represented in TF and planning inputs.
    authority: [piper_description.xacro, calibration_result.yaml, publish_hand_eye_tf.py]

  - id: selectable_camera_profiles
    status: [ACTIVE, SOFTWARE_QUALIFIED]
    summary: >-
      GUI controls persist bounded RGB resolution and frame-rate choices for
      the next newly started camera process without changing the public mission
      action or mutating a live stream. 640x480 at 30 Hz remains the
      operationally evidenced profile while higher RGB modes are available for
      benchmarking and calibration.
    authority: [piper_gui_native.py, piper_gui/camera_profile.py, L515_camera/rgb_profile.conf, start_l515_camera.sh, start_l515_camera_low_bandwidth.sh]

  - id: baseline_offline_masked_tsdf
    status: [ACTIVE, SOFTWARE_QUALIFIED]
    summary: >-
      Successful adaptive datasets can be checked for manifest/frame-count and
      capture-time transform consistency and fused offline into a masked TSDF
      with a quality report. This baseline is not yet a proven good mesh.
    authority: [reconstruction/tsdf_reconstruct.py, reconstruction_jobs.py]

later_feature_reintegration_status:
  - id: direct_configured_home_without_tesseract
    status: [ACTIVE, SOFTWARE_REQUALIFIED, PHYSICAL_REQUALIFICATION_REQUIRED]
    summary: >-
      Typed ExecuteHomeStage moved STARTUP_WRIST, PRE_HOME, ROUGH_HOME and
      STORAGE_WRIST out of Tesseract and executed authenticated direct MoveJ
      endpoints with feedback, direction, motor, external-clearance and settle checks; camera/tracking/workflow/obstacle freshness and Tesseract controller-limit hashes are not direct-home authority.
      STARTUP_WRIST alone is explicitly J6-owned: J1-J5 refresh from measured
      feedback at each command boundary, progress and settling use J6 only,
      and no redundant hold is inserted before full-joint ROUGH_HOME.
    authority: [piper_mobile_manipulation/srv/ExecuteHomeStage.srv, scan_viewpoint_executor_node.py, target_scan_mission_node.py]

  - id: schema4_pre_home_waypoint
    status: [ACTIVE, SOFTWARE_REQUALIFIED, PHYSICAL_REQUALIFICATION_REQUIRED]
    summary: >-
      A recorded PRE_HOME waypoint was inserted only in the return sequence
      before rough home to avoid the J5/camera-holder collision posture.
    authority: [piper_home_pose.json, home_pose.py, mission_core.py, target_scan_mission_node.py]

  - id: startup_j6_positive_only_wrap_bridge
    status: [ACTIVE, SOFTWARE_REQUALIFIED, PHYSICAL_REQUALIFICATION_REQUIRED]
    summary: >-
      Driver-owned startup logic represented the negative storage branch,
      allowed only positive rotation, used the qualified raw wrap bridge and
      retained its controller-turn offset until terminal storage. Proved
      disable and direction-trip paths reset incomplete transaction/high-water
      state before any later commissioning enable. The archived executor-side
      J6-only progress/settle and command-boundary refresh semantics are now
      cleanly restored in the current branch after the August 16 E2E regression.
    authority: [piper_ctrl_single_node.py, scan_viewpoint_executor_node.py]

  - id: guarded_zero_capture_dataset_deletion
    status: [ACTIVE, SOFTWARE_REQUALIFIED]
    summary: >-
      Failed missions with zero accepted captures removed only their exact
      identity-bound incomplete dataset after the writer stopped; historical
      datasets and any folder containing accepted artifacts were preserved.
    authority: [target_scan_mission_node.py]

  - id: reachable_full_sphere_nbv_v2
    status: [ARCHIVED_LATER, SOFTWARE_QUALIFIED]
    summary: >-
      Full-sphere candidate directions, adaptive profile-aware distance,
      achieved acquisition-pose graph seeding, hard 10-degree ray de-duplication,
      reachable-bin coverage, capture-skipping transits and bounded alternative
      planning replaced repeated local-hemisphere selections.
    source: /home/prl/Piper_arm_previous_version/piper_ros_foxy/src/piper_mobile_manipulation

  - id: nbv_planning_budget_recovery
    status: [ARCHIVED_LATER, SOFTWARE_QUALIFIED]
    summary: >-
      Stable 810-candidate envelopes were cached, planner work paused while a
      request was active, exhausted candidate IDs were reported and excluded,
      and at most three complete transactions were allowed for one view.
    source: /home/prl/Piper_arm_previous_version

  - id: shared_depth_layer_selector
    status: [ACTIVE, SOFTWARE_REQUALIFIED, LIVE_SENSOR_REQUALIFICATION_REQUIRED]
    summary: >-
      One ambiguity-aware target-depth selector served live target geometry,
      stable landmark, occlusion fallback and capture, preventing SAM boundary
      pixels from switching between foreground target and background layers.
    source: /home/prl/Piper_arm_previous_version/piper_ros_foxy/src/piper_mobile_manipulation/piper_mobile_manipulation/utils/target_depth.py

  - id: confidence_qualified_capture_schema2
    status: [ACTIVE, SOFTWARE_REQUALIFIED, PARTIAL]
    summary: >-
      Native L515 depth/confidence synchronization, exact RGB-mask identity,
      adaptive erosion, depth-continuous primary components and immutable
      confidence artifacts strengthened capture acceptance. A motors-disabled
      native stream test passed after adding a CameraInfo startup barrier;
      semantic target jitter and persisted schema-2 capture remain to qualify.
    source: /home/prl/Piper_arm_previous_version

  - id: settled_twenty_frame_depth_burst
    status: [ACTIVE, SOFTWARE_REQUALIFIED, LIVE_SENSOR_REQUALIFICATION_REQUIRED]
    summary: >-
      Every accepted viewpoint now requires a trailing settled burst of twenty
      synchronized native L515 depth/confidence observations. Nonzero
      grade-8-or-better samples are fused by a per-pixel median with ten-frame
      minimum support before the existing target-component qualification; the
      exact mask-correlated RGB remains the representative colour frame.
    authority: [scan_capture.py, scan_capture_node.py, scan_capture_params.yaml]

  - id: one_millimetre_target_geometry
    status: [ACTIVE, SOFTWARE_REQUALIFIED, PHYSICAL_REQUALIFICATION_REQUIRED]
    summary: >-
      Accepted refined target clouds retain every masked depth pixel into 1 mm
      cells and offline TSDF defaults to 1 mm with a 5 mm truncation band.
      Native schema-2 depth remains un-downsampled, while the separate NBV and
      scene grids deliberately remain 5 mm and 15 mm respectively.
    authority: [target_cloud_node.py, run_target_cloud.sh, tsdf_reconstruct.py]

  - id: smooth_direct_movej_or_streamed_detour
    status: [ACTIVE, SOFTWARE_REQUALIFIED, PHYSICAL_REQUALIFICATION_REQUIRED]
    summary: >-
      A densely proven straight joint chord could use one native MoveJ endpoint
      for continuous controller interpolation; a route needing collision
      avoidance retained its complete Tesseract stream and detour geometry.
    authority: [worker.py, scan_viewpoint_executor_node.py]

  - id: pass_through_stream_smoothing
    status: [ACTIVE, SOFTWARE_REQUALIFIED, PHYSICAL_REQUALIFICATION_REQUIRED]
    summary: >-
      Bounded internal path blending reduced stop-at-waypoint behavior while
      preserving exact start/end/reversal points and validating the actual
      blended geometry before execution.
    authority: [worker.py, scan_viewpoint_executor_node.py]

  - id: tsdf_bounded_gicp_and_gui_validation
    status: [ACTIVE, SOFTWARE_REQUALIFIED]
    summary: >-
      Reconstruction compared robot-pose TSDF with bounded local GICP, enforced
      manifest/calibration/confidence provenance, produced PLY/quality reports,
      and added command-free GUI previews and Open3D inspection.
    authority: [reconstruction/tsdf_reconstruct.py, reconstruction/gui_support.py, reconstruction/view_reconstruction.py, piper_gui_native.py]

known_incomplete_or_unproven:
  - Complete physical E2E success from target submission through diverse scan, home, disable and cleanup has not been re-established after restoring the rollback.
  - Physical branch/leaf removal by either push or pick/place is not qualified; manipulation_model_qualified remains false.
  - Safe-area inference for real placement has proposal logic but no accepted physical end-to-end evidence.
  - NBV has produced useful paths and captures, but repeatable both-Y-side/full reachable-coverage completion remains physically unproven.
  - Reachable full-sphere NBV v2 and its planning-budget recovery remain archived. The cleaned architecture does not by itself correct local-frontier exhaustion, repeated same-sector selection, or repeated candidate recomputation.
  - The archived confidence-qualified TSDF/GICP implementation rejected the available 15-view and 24-view datasets for duplicated or oversized geometry; no optimizer was approved.
  - Outdoor wind, rigid target motion and non-rigid deformation handling are archived design work only.
  - The tracked robot transport/odometry/base-home handshake is represented by typed interfaces and the GUI substitute, not a completed two-machine deployment.
  - Gazebo or Isaac simulation and sim-to-real occlusion-removal validation are not implemented as an accepted pipeline.

rewrite_rule: >-
  Treat this file as a preservation checklist. Reintroduce archived-later items
  in small subsystem patches only after the active rollback behavior is
  measured. Do not copy the archived integration wholesale, do not weaken
  safety gates to recover progress, and update this bookmark whenever a feature
  becomes active, is rejected, or gains new physical evidence.

selected_reintegration_2026_08_15:
  branch: reintegrate/selected-archived-features
  active_features: [direct_configured_home_without_tesseract, schema4_pre_home_waypoint, startup_j6_positive_only_wrap_bridge, guarded_zero_capture_dataset_deletion, smooth_direct_movej_or_streamed_detour, pass_through_stream_smoothing, tsdf_bounded_gicp_and_gui_validation, shared_depth_layer_selector, confidence_qualified_capture_schema2]
  deferred_features: [reachable_full_sphere_nbv_v2, nbv_planning_budget_recovery]
  physical_status: No physical command was executed during reintegration. Every motion-affecting feature requires the staged supervised checklist before use.

live_voxel_nbv_2026_08_17:
  state: authoritative_selected_awaiting_supervised_E2E
  replaces_deferred_item: reachable_full_sphere_nbv_v2
  implemented: [schema2 voxel evidence, immutable coverage snapshot, visible unknown raycast, novel surface direction score, information-before-travel ordering, unique-direction-first radius fallbacks, recorded replay]
  default_policy: superseded_by_ray_nbv_default_2026_08_21
  authority_files: [nbv_coverage.py, scan_viewpoint_planner_node.py, piper_tesseract_foxy/bridge_node.py]
  physical_status: Not yet qualified. The operator explicitly selected authoritative voxel_nbv for the next supervised E2E on 2026-08-17; its outcome must be reviewed before qualification is claimed.

post_enable_startup_stability_2026_08_25:
  state: software_implemented_physical_retest_pending
  capability: First STARTUP_WRIST cannot reuse disabled-state stability; new post-enable samples must prove two continuous seconds finite stationary, controller-fault-free and all-six-enabled.
  failure_behavior: Intact-control failure proves disable before wrist motion; current motor-authority loss retains watchdog-owned no-command containment.
  preserved: [direct-home relaxation buffer, measured J1-J5 preservation, positive-only J6 startup, wrist completion before simultaneous rough home, in-motion fault stop]

architecture_cleanup_2026_08_27:
  state: implementation_and_final_regression_complete_push_pending
  branch: reintegrate/selected-archived-features
  implemented: [mission and infrastructure responsibility packages, execution package, planning package, perception package, Tesseract candidate and protocol boundaries, native GUI ROS adapter, reconstruction input provenance, conservative driver joint-state policy seam, responsibility-grouped development tests, historical-doc routing, large-asset policy]
  preserved: [ROS and GUI external interfaces, configuration and launch semantics, mission safety planner driver reconstruction and dataset behavior, root operator commands, runtime asset paths]
  evidence: docs/refactor/architecture_cleanup_2026_08_27.md
  final_deliverables: [ARCHITECTURE.md, docs/refactor/final_equivalence_report.md, docs/architecture/asset_policy.md, docs/ai/project-structure.txt]

generic_motion_planner_and_curobo_2026_08_27:
  state: software_and_isolated_gpu_runtime_implemented_collision_geometry_qualification_pending
  branch: curobo-integration
  implemented: [PlannerBackend and frozen PlannerConfig, generic RequestMotionPlan and MotionPlan ROS contracts, backend-neutral timed execution validation, one shared mission authorization safety capture and execution path, Tesseract adapter and compatibility aliases, ROS-free cuRobo MotionGen worker, GUI next-mission selection, process supervision, pure and opt-in GPU tests]
  pinned_curobo: {version: 0.7.8, commit: d64c4b005459db10c5dd867d8b30a87d5bda9bdb, warp: 1.11.1}
  runtime_evidence: [Python 3.10.20, PyTorch 2.11.0+cu128, CUDA toolkit 12.8.93, RTX 3090, native extensions, MotionGen warm-up, free-space command-free plan, healthy worker, bounded cleanup]
  fail_closed_status: cuRobo worker runtime is available, but collision_model_qualified remains false because canonical-platform planning reproduces a Bunker-cuboid DT_EXCEPTION; physical missions remain blocked.
  unchanged: [one mission implementation, NBV, PlanAuthorizer, runtime safety gates, TrajectoryRunner, PiPER driver path, default-disabled real motion, no planner fallback]
