# Calibration tools

These Python programs implement the guarded calibration entry points under
`scripts/calibration/`. Run the shell entry points from the repository root so
the canonical Foxy workspace and root `piper_joint_bounds.json` are used.
